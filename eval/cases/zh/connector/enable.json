{
  "id": "connector-enable",
  "lang": "zh",
  "category": "connector",
  "workflow": "connector-management",
  "scenario_type": "single_turn",
  "description": "Create a connector then enable it (covers `heramind connector enable`)",
  "setup": {
    "fixture": "seed-default"
  },
  "turns": [
    {
      "user": "创建一个名为 conn-1 的 MQTT connector,然后启用它。"
    }
  ],
  "applies": [
    "tool_accuracy",
    "task_completion",
    "language_adherence"
  ],
  "expectations": {
    "per_turn": [
      "Turn 1: Create a connector then enable it (covers `heramind connector enable`)"
    ],
    "overall": "Create a connector then enable it (covers `heramind connector enable`)"
  }
}
