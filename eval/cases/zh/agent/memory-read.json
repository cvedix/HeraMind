{
  "id": "agent-memory-read",
  "lang": "zh",
  "category": "agent",
  "workflow": "agent-management",
  "scenario_type": "single_turn",
  "description": "Create an agent then read its memory journal (covers `heramind agent memory`)",
  "setup": {
    "fixture": "seed-default"
  },
  "turns": [
    {
      "user": "创建一个名为 probe-one 的 agent,每 5 分钟检查一次 sensor-001,然后读取它的记忆日志。"
    }
  ],
  "applies": [
    "tool_accuracy",
    "task_completion",
    "language_adherence"
  ],
  "expectations": {
    "per_turn": [
      "Turn 1: Create an agent then read its memory journal (covers `heramind agent memory`)"
    ],
    "overall": "Create an agent then read its memory journal (covers `heramind agent memory`)"
  },
  "state_queries": [
    {
      "type": "agent_exists",
      "params": {
        "id": "probe-one"
      },
      "expected": true
    }
  ]
}
