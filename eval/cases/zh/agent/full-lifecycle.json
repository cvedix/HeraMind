{
  "id": "agent-full-lifecycle",
  "lang": "zh",
  "category": "agent",
  "workflow": "agent-management",
  "scenario_type": "single_turn",
  "description": "Create an agent, execute it, then delete it (multi-step lifecycle)",
  "setup": {
    "fixture": "seed-default"
  },
  "turns": [
    {
      "user": "创建一个名为 lifecycle-probe 的 agent 每 10 分钟检查 sensor-001,执行一次,然后删除它。"
    }
  ],
  "applies": [
    "tool_accuracy",
    "task_completion",
    "language_adherence"
  ],
  "expectations": {
    "per_turn": [
      "Turn 1: Create an agent, execute it, then delete it (multi-step lifecycle)"
    ],
    "overall": "Create an agent, execute it, then delete it (multi-step lifecycle)"
  }
}
