{
  "id": "rule-fire-runtime",
  "lang": "en",
  "category": "rule",
  "workflow": "rule-runtime",
  "scenario_type": "single_turn",
  "description": "Rule runtime: query current metric value (variation 2).",
  "setup": {
    "fixture": "scenario-rule-runtime"
  },
  "turns": [
    {
      "user": "Check what webhook-sensor's battery_level is right now."
    }
  ],
  "applies": [
    "tool_accuracy",
    "task_completion",
    "language_adherence"
  ],
  "expectations": {
    "per_turn": [
      "T1: agent reads current battery_level"
    ],
    "overall": "(runtime 注入 battery_level=10 后)预置规则 fire 产生告警消息"
  },
  "expect": {
    "tools": [
      "shell"
    ],
    "commands": [
      "heramind device get"
    ]
  }
}