{
  "id": "device-webhook-url",
  "lang": "en",
  "category": "device",
  "workflow": "device-management",
  "scenario_type": "single_turn",
  "description": "Read a device's webhook ingest URL (covers `heramind device webhook-url`)",
  "setup": {
    "fixture": "seed-default"
  },
  "turns": [
    {
      "user": "What is the webhook telemetry ingest URL for device sensor-001?"
    }
  ],
  "applies": [
    "tool_accuracy",
    "task_completion",
    "language_adherence"
  ],
  "expectations": {
    "per_turn": [
      "Turn 1: Read a device's webhook ingest URL (covers `heramind device webhook-url`)"
    ],
    "overall": "Read a device's webhook ingest URL (covers `heramind device webhook-url`)"
  }
}
