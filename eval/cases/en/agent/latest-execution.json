{
  "id": "agent-latest-execution",
  "lang": "en",
  "category": "agent",
  "workflow": "agent-management",
  "scenario_type": "single_turn",
  "description": "Create an agent, execute it, then show latest execution (covers `heramind agent latest-execution`)",
  "setup": {
    "fixture": "seed-default"
  },
  "turns": [
    {
      "user": "Create an agent named probe-two that watches sensor-001, execute it once now, then show its latest execution."
    }
  ],
  "applies": [
    "tool_accuracy",
    "task_completion",
    "language_adherence"
  ],
  "expectations": {
    "per_turn": [
      "Turn 1: Create an agent, execute it, then show latest execution (covers `heramind agent latest-execution`)"
    ],
    "overall": "Create an agent, execute it, then show latest execution (covers `heramind agent latest-execution`)"
  },
  "state_queries": [
    {
      "type": "agent_exists",
      "params": {
        "id": "probe-two"
      },
      "expected": true
    }
  ]
}
