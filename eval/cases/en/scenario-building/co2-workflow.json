{
  "id": "building-co2-workflow",
  "lang": "en",
  "category": "scenario",
  "workflow": "scenario-building",
  "scenario_type": "single_turn",
  "description": "en version of building-co2-workflow",
  "setup": {
    "fixture": "scenario-building"
  },
  "turns": [
    {
      "user": "Check all rooms. For any room with CO2 (co2_ppm) above 1000, send set_mode ventilate to its HVAC, and create an alert rule named building-room-a-co2-alert."
    }
  ],
  "applies": [
    "tool_accuracy",
    "task_completion",
    "language_adherence"
  ],
  "expectations": {
    "per_turn": [
      "Turn 1: device control hvac-a set_mode + rule create building-room-a-co2-alert"
    ],
    "overall": "set_mode sent to hvac-a + alert rule created - both land"
  },
  "state_queries": [
    {
      "type": "device_command_sent",
      "params": {
        "id": "hvac-a",
        "command": "set_mode"
      },
      "expected": true
    },
    {
      "type": "rule_exists",
      "params": {
        "name": "building-room-a-co2-alert"
      },
      "expected": true
    }
  ],
  "expect": {
    "tools": [
      "shell"
    ],
    "commands": [
      "heramind device control"
    ]
  }
}