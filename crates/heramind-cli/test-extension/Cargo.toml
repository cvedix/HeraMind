[package]
name = "heramind_test_extension"
version = "0.1.0"
edition = "2021"

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
heramind-extension-sdk = "0.6.3"
async-trait = "0.1"
serde_json = "1.0"
tokio = { version = "1", features = ["rt-multi-thread", "sync"] }
