[Unit]
Description=HeraMind Server
Documentation=https://github.com/cvedix/HeraMind
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
User=heramind
Group=heramind

# Working directory
WorkingDirectory=/var/lib/heramind

# Executables — `serve` is required; the install script generates the same
# unit inline with the chosen host/port.
ExecStart=/usr/local/bin/heramind serve --host 0.0.0.0 --port 9375
ExecReload=/bin/kill -HUP $MAINPID

# Auto-restart
Restart=always
RestartSec=5
StartLimitBurst=10
StartLimitInterval=60

# Environment
Environment="RUST_LOG=info"
Environment="HERAMIND_DATA_DIR=/var/lib/heramind"

# Security settings
# Note: web-triggered upgrades (About 页 → check for updates) do NOT relax
# these — the sandboxed service can't swap its own binary, so it stages the
# release under /var/lib/heramind/data/upgrade/ and writes apply.trigger,
# which the root heramind-upgrade-apply.path unit (written by install.sh)
# watches.
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true
ReadWritePaths=/var/lib/heramind
RestrictRealtime=true
RestrictAddressFamilies=AF_INET AF_INET6 AF_UNIX

# Resource limits
LimitNOFILE=65536
MemoryMax=2G

[Install]
WantedBy=multi-user.target
