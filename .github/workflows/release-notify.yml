name: Release Notify

# Posts a release announcement to the Discord community channel. Triggered by
# the build workflow's release job (which dispatches us with the tag once the
# GitHub Release is published); also runnable manually via workflow_dispatch.
#
# NOT triggered by `on: release: published`: a release published via the UI or
# a PAT emits that event and would fire this workflow IN ADDITION to build's
# explicit dispatch → duplicate Discord posts (hit on v0.9.14: 3 posts).
# Dispatch is the single trigger source.
#
# Requires the `DISCORD_WEBHOOK_URL` repo secret (Discord channel Webhook URL).
# Optional `DISCORD_ROLE_ID`, when set, adds a role ping to the message.

on:
  workflow_dispatch:
    inputs:
      tag_name:
        description: "Release tag to announce (e.g. v0.9.14)"
        required: true

permissions:
  contents: read

jobs:
  notify:
    name: Post to Discord
    runs-on: ubuntu-latest
    steps:
      - name: Checkout tag (for CHANGELOG extractor)
        uses: actions/checkout@v4
        with:
          ref: refs/tags/${{ github.event.release.tag_name || inputs.tag_name }}

      - name: Resolve release metadata
        id: meta
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG_NAME: ${{ github.event.release.tag_name || inputs.tag_name }}
          HTML_URL: ${{ github.event.release.html_url }}
          BODY: ${{ github.event.release.body }}
        run: |
          tag="${TAG_NAME}"
          url="${HTML_URL:-https://github.com/${GITHUB_REPOSITORY}/releases/tag/${tag}}"
          VERSION="${tag#v}"

          # Prefer the curated SHORT changelog — the same content the OTA
          # dialog shows, so Discord and the in-app updater stay in sync.
          # The build workflow dispatches us with the tag; we checked it out
          # above so CHANGELOG.md + the extractor are present at that ref.
          NOTES="$(python3 scripts/extract_changelog.py --version "${VERSION}" --mode short 2>/dev/null || true)"

          # Fallback: extractor missing / version not in CHANGELOG -> use the
          # release body. On workflow_dispatch github.event.release.* is empty
          # (a release published with the default GITHUB_TOKEN doesn't emit a
          # `release: published` event that re-triggers workflows), so fetch it.
          if [ -z "${NOTES}" ]; then
            if [ -z "${BODY}" ]; then
              fetched=$(gh release view "${tag}" --repo "${GITHUB_REPOSITORY}" --json body,htmlUrl 2>/dev/null || echo '{}')
              BODY=$(printf '%s' "${fetched}" | jq -r '.body // ""')
              fetched_url=$(printf '%s' "${fetched}" | jq -r '.htmlUrl // ""')
              [ -n "${fetched_url}" ] && url="${fetched_url}"
            fi
            NOTES="${BODY}"
          fi

          echo "tag=${tag}" >> "$GITHUB_OUTPUT"
          echo "url=${url}" >> "$GITHUB_OUTPUT"

          # Read NOTES via the environment so multi-line newlines survive into
          # the JSON payload. Truncate well under Discord's 4096 embed cap.
          export RELEASE_NOTES="${NOTES}"
          python3 - <<'PY'
          import json, os
          notes = os.environ.get("RELEASE_NOTES", "") or ""
          summary = notes.strip()
          if len(summary) > 1500:
              summary = summary[:1497] + "…"
          with open(os.environ["GITHUB_ENV"], "a") as f:
              f.write("EMBED_SUMMARY_JSON=" + json.dumps(summary) + "\n")
          PY

      - name: Post announcement to Discord
        env:
          DISCORD_WEBHOOK_URL: ${{ secrets.DISCORD_WEBHOOK_URL }}
          DISCORD_ROLE_ID: ${{ secrets.DISCORD_ROLE_ID }}
          TAG: ${{ steps.meta.outputs.tag }}
          URL: ${{ steps.meta.outputs.url }}
          SUMMARY_JSON: ${{ env.EMBED_SUMMARY_JSON }}
        run: |
          if [ -z "$DISCORD_WEBHOOK_URL" ]; then
            echo "::warning::DISCORD_WEBHOOK_URL secret is not set; skipping Discord announcement."
            exit 0
          fi

          # Build the optional role-ping prefix.
          ROLE_PREFIX=""
          if [ -n "$DISCORD_ROLE_ID" ] && [ "$DISCORD_ROLE_ID" != "0" ]; then
            ROLE_PREFIX="<@&${DISCORD_ROLE_ID}> "
          fi

          # Assemble the Discord webhook payload via jq for robust JSON escaping.
          # `content` holds the ping + headline; `embeds[0].description` holds the
          # release notes summary. Embed colour = HeraMind-ish blue (#2563eb).
          PAYLOAD=$(jq -n \
            --arg content "${ROLE_PREFIX}🚀 **HeraMind ${TAG}** is out!" \
            --arg url "$URL" \
            --arg tag "$TAG" \
            --argjson summary "${SUMMARY_JSON:-\"\"}" \
            '{
              content: $content,
              allowed_mentions: { parse: ["roles", "everyone"] },
              embeds: [{
                title: ("Release " + $tag),
                url: $url,
                description: $summary,
                color: 2446977,
                footer: { text: "HeraMind Release Feed" },
                timestamp: (now | todate)
              }]
            }')

          echo "::add-mask::$DISCORD_WEBHOOK_URL"
          echo "Sending Discord payload for tag ${TAG}…"

          # Fire the webhook. Discord returns 204 No Content on success.
          # `--fail-with-body` surfaces rate-limit / 4xx errors with the body.
          code=$(curl -sS -o /tmp/discord_resp.txt -w "%{http_code}" \
            --fail-with-body \
            -X POST \
            -H "Content-Type: application/json" \
            -d "$PAYLOAD" \
            "$DISCORD_WEBHOOK_URL" || true)

          echo "Discord responded HTTP ${code}"
          cat /tmp/discord_resp.txt
          echo

          if [ "$code" != "204" ] && [ "$code" != "200" ]; then
            echo "::error::Discord webhook failed (HTTP ${code})"
            exit 1
          fi
